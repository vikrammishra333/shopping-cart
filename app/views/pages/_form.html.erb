<%= form_for(@user, :remote => true) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<table align="center">
  <tr class="field">
    <td class="fld_label"><%= f.label :name, "Name:"  %></td>
    <td class="fld_input"><%= f.text_field :name %></td>
  </tr>
  <tr class="field">
    <td class="fld_label"><%= f.label :email, "Email:" %></td>
    <td class="fld_input"><%= f.text_field :email %></td>
  </tr>
  <tr class="field">
    <td class="fld_label"><%= f.label :password , "Password:" %></td>
   <td class="fld_input"> <%= f.password_field :password%></td>
  </tr>
  <tr class="field">
    <td class="fld_label"><%= f.label :password_confirmation, "Confirm Password:" %>
    <td class="fld_input"><%= f.password_field :password_confirmation %>
  </tr>
  <tr class="actions">
    <td class="fld_label"> </td>
    <td class="fld_input"><%= f.submit "Sign up" %></td>
  </tr>
</table>
<% end %>
